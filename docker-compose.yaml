version: '3.8'
services:
  nginx:
    image: nginx:latest
    restart: always
    container_name: nginx
    volumes:
      # - ./nginx:/usr/share/nginx/html
      # - ./nginx:/temporary
      - ./nginx:/etc/nginx
      - /opt/cache/:/opt/cache/
    ports:
      - "80:80"
      - "443:443"
