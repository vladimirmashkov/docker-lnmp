version: '3.8'
services:
  nginx:
    image: nginx:latest
    restart: always
    container_name: nginx
    volumes:
      - ./nginx:/etc/nginx
      - /etc/resolv.conf:/etc/resolv.conf
      - /etc/hosts:/etc/hosts
      - /opt/cache/:/opt/cache/
    ports:
      - "80:80"
      - "443:443"
